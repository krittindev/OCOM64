#include<bits/stdc++.h>
using namespace std;
class Point{
    public:
    float x, y;
};
class Rec{
    public:
    Point edge[4];
    bool isIn(Rec other){
        if(other.edge[0].x)
    }
};
int main(){
    Rec a, b;
    for(int i = 0; i < 4; i++)
        cin >> a.edge[i].x >> a.edge[i].y;
    for(int i = 0; i < 4; i++)
        cin >> b.edge[i].x >> b.edge[i].y;
    a.calMiddle();
    b.calMiddle();
    return 0;
}

