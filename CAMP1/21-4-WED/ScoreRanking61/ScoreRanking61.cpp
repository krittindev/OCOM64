#include<bits/stdc++.h>
using namespace std;
int main(){
	vector<vector<int>> table(26, vector<int>(5, 0));
	int n, r, sc1, sca, scb;
	char a, b;
	string alp;
	cin >> n;
	for(int i = 0; i < n; i++)
		cin >> alp[i];
	cin >> r;
	
	return 0;
}
